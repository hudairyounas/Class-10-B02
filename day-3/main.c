#include <stdio.h>

void main()
{
    int num = 10;

    if (num > 0)
    {
        printf("Number is Positive");
    }
    else if (num < 0)
    {
        printf("Number is Negative");
    }
    else
    {
        printf("Number is Negative");
    }

    //! Tesla interview Question

    //! 100 prisoners. they all are in 1 direction. every prisoners have wear only blue or red hat and every prisoner have only see the next person hat color not its and and the person wo stand on the behalf of it. now the officer went their and check all the hat color and if the prisoners tell the right answer that what its hat color than the prisoner will go otherwise the prisoner will kill them.

    //! so the question is which strategy that all the prisoners that make in which maximum number of prisoners will safe.
}